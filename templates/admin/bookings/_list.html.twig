<div class="result-count">{{ attendees|length }} {{ attendees|length == 1 ? 'booking' : 'bookings' }}</div>

{% if attendees is empty %}
  <div class="empty">
    <strong>No bookings found</strong>
    Try adjusting your search.
  </div>
{% else %}
  <div class="table-scroll">
    <table class="admin-table">
      <thead>
        <tr>
          <th>Member</th>
          <th>Event</th>
          <th>Date</th>
          <th>Status</th>
          <th>Checked in</th>
          <th>Method</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        {% for attendee in attendees %}
          {% set displayName = attendee.user.displayName %}
          {% set pillClass = attendee.status == 'confirmed' ? ' status-pill--published' : (attendee.status == 'cancelled' ? ' status-pill--cancelled' : (attendee.status == 'waiting' ? ' status-pill--waiting' : '')) %}
          <tr>
            <td>
              <a href="{{ path('app_admin_user_show', {id: attendee.user.id}) }}" class="admin-table__link">
                {{ displayName ?: attendee.user.email }}
              </a>
            </td>
            <td>
              <a href="{{ path('app_admin_event_show', attendee.event.isRecurring ? {id: attendee.event.id, date: (attendee.occurrenceDate ?? attendee.event.date)|date('Y-m-d')} : {id: attendee.event.id}) }}" class="admin-table__link">
                {{ attendee.event.title }}
              </a>
            </td>
            <td class="admin-table__muted">{{ (attendee.occurrenceDate ?? attendee.event.date)|date('d M Y') }}</td>
            <td>
              <span class="status-pill{{ pillClass }}">{{ attendee.status|capitalize }}</span>
            </td>
            <td class="admin-table__muted">
              {{ attendee.isCheckedIn ? attendee.checkedInAt|date('d M Y, H:i') : '—' }}
            </td>
            <td class="admin-table__muted">
              {{ attendee.isCheckedIn ? (attendee.checkedInMethod == 'door_pin' ? 'Door PIN' : 'Manual') : '—' }}
            </td>
            <td><a href="{{ path('app_admin_booking_edit', {id: attendee.id}) }}" class="admin-table__link">Edit</a></td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
{% endif %}
